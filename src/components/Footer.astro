---
import { SITE_AUTHOR } from '../consts';

const today = new Date();
---

<footer>
	<div class="footer-inner">
		<span class="copyright">&copy; {today.getFullYear()} {SITE_AUTHOR.name}</span>
		<div class="footer-links">
			<a href={SITE_AUTHOR.github.url} target="_blank" rel="noopener">GitHub</a>
			<a href={`mailto:${SITE_AUTHOR.email}`}>Email</a>
		</div>
	</div>
</footer>
<style>
	footer {
		border-top: 1px solid var(--border);
		background: var(--header-bg);
		transition: background-color 0.3s ease;
	}
	.footer-inner {
		max-width: 760px;
		margin: auto;
		padding: 1.5em 1em;
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1em;
		flex-wrap: wrap;
	}
	.copyright {
		font-size: 0.875em;
		color: var(--text-muted);
	}
	.footer-links {
		display: flex;
		gap: 1.25em;
	}
	.footer-links a {
		font-size: 0.875em;
		color: var(--text-muted);
		text-decoration: none;
		transition: color 0.2s ease;
	}
	.footer-links a:hover {
		color: var(--accent);
		text-decoration: none;
	}
</style>
